# ── Database (MySQL) ──────────────────────────────────────────
DB_HOST=mysql
DB_NAME=id19066703_pfe_users
DB_USER=id19066703_pfe
DB_PASSWORD=change_me

# ── Application URLs ──────────────────────────────────────────
# Angular front-end (used by PHP emails for activation/reset links)
APP_FRONT_URL=http://localhost:8080
# PHP API base URL (used by Angular via environment.ts)
APP_API_URL=http://localhost:8081
# Elasticsearch base URL (used by Angular via environment.ts)
ES_URL=http://localhost:9200/echantillon/_search

# ── Mail (MailPit SMTP) ───────────────────────────────────────
MAIL_HOST=mailpit
MAIL_PORT=1025
MAIL_FROM=noreply@dataap.local
